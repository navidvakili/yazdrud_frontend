// ============================================================
// TutsModule — Shared Custom Hooks (Backward-compatible re-exports)
// All hooks have been moved to ./hooks/ for modularity.
// ============================================================

export {
  useToast,
  useVoucherOps,
  usePreRegistration,
  useSurveyOps,
  useReceiptOps,
  usePagination,
  useStatsFilter,
} from './hooks';

// All hook implementations have been moved to individual files in ./hooks/
